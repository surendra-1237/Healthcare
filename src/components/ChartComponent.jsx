const ChartComponent = () => {
  // placeholder chart area. Replace with real chart (Chart.js, Recharts, etc.)
  return (
    <div style={{height:220, background:'#fafafa', border:'1px dashed #ddd', borderRadius:8, display:'flex', alignItems:'center', justifyContent:'center'}}>
      <div style={{color:'#888'}}>Chart placeholder</div>
    </div>
  );
};

export default ChartComponent;
